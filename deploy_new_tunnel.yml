---
- name: Deploy New Tunnel to Production CSR
  hosts: all
  gather_facts: no
  connection: network_cli

  tasks:
    - name: Push Additive Configuration
      cisco.ios.ios_config:
        lines: "{{ lookup('template', 'templates/add_tunnel.j2') }}"
