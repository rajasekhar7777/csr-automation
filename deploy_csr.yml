---
- name: Modular CSR Configuration
  hosts: all
  gather_facts: no
  # Connection should be managed by variables in AWX, but you can force it here
  tasks:
    - name: Generate and Apply Configuration
      cisco.ios.ios_config:
        lines: "{{ lookup('template', 'master_template.j2') }}"
